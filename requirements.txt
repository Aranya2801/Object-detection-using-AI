# ─────────────────────────────────────────────────────────────────
#  Object-Detection-using-AI — Requirements
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# Core — YOLO11 / YOLOv8 / v9 / v10 via Ultralytics
ultralytics>=8.3.0          # YOLO11, YOLOv8, v9, v10 unified API

# Computer Vision
opencv-python>=4.10.0        # Image/video I/O and rendering
opencv-contrib-python>=4.10.0  # Extra modules (optional, for SIFT etc.)
Pillow>=10.4.0               # Image utilities

# Deep Learning (choose one GPU backend)
torch>=2.4.0                 # PyTorch — CUDA/CPU/MPS
torchvision>=0.19.0          # Vision transforms
# torch>=2.4.0+cu121         # CUDA 12.1 (uncomment for NVIDIA GPU)

# Data / Analytics
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0

# Visualisation (optional, for notebooks/dashboards)
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.23.0

# Export formats
onnx>=1.16.0                 # ONNX export
onnxruntime>=1.18.0          # ONNX CPU inference

# Configuration & CLI
PyYAML>=6.0.2
tqdm>=4.66.0
rich>=13.8.0                 # Beautiful terminal output

# Tracking (bundled with ultralytics, listed for clarity)
# lapx>=0.5.9               # ByteTrack dependency (auto-installed)

# Development & testing
pytest>=8.3.0
pytest-cov>=5.0.0

# Jupyter notebooks (optional)
jupyter>=1.1.0
ipywidgets>=8.1.0
